# 玄学辅助建议治理

版本：`metaphysical-advice-governance@1.0`
实现：`mingli.rule_governance.AdviceRule`

## 1. 定位

玄学辅助是现实行动之后的低风险文化模块，不是效果承诺、医疗服务、投资建议、招录保证或关系操控工具。`production` 表示该建议通过本地结构与安全合同，可以被付费 Renderer 选择；`evidence_status=traditional_culture_only` 明确表示它没有被提升为经验或科学效果证据。

## 2. AdviceRule 合同

每条规则必须完整包含：

| 字段 | 含义 |
|---|---|
| `advice_id` | 稳定、唯一的建议 ID |
| `domain` | 九类专题之一 |
| `triggers` | 允许匹配的条件 |
| `contraindications` | 禁忌和停止条件 |
| `required_inputs` | 给建议前必须掌握的资料 |
| `metaphysical_rationale` | 传统文化语境，不得写成效果证据 |
| `real_world_problem` | 该建议实际面对的现实问题 |
| `recommended_action` | 低风险、可退出的动作 |
| `execution_steps` | 有限、透明的执行步骤 |
| `frequency_cap` | 次数上限，禁止连续追加 |
| `budget_cap` | 预算上限，禁止借款 |
| `required_real_world_actions` | 同时必须完成的现实动作 |
| `prohibited_claims` | 结果保证与替代性宣称黑名单 |
| `scam_red_flags` | 恐吓、追加、借钱和不透明收费红旗 |
| `evidence_status` | 当前固定为 `traditional_culture_only` |

此外，每条规则还必须具备 `status`、`source_refs`、`applicability`、`counterexamples` 和 `review`。缺任一治理字段即目录校验失败。

## 3. 主题映射

| 专题 | 允许的文化辅助 | 强制现实动作 |
|---|---|---|
| 财运 | 守财、招财象征、开市前复核、财位整理；补财库只在明确点名且无财务压力时单次考虑 | 现金流表、应急金、预算和风险上限 |
| 感情 | 姻缘、桃花语境、沟通择时、卧室整理 | 核对婚恋与联系边界、处理分手根因、尊重拒绝 |
| 考试 | 启文昌、文昌祈福、开笔、学习位 | 真实成绩、计时练习、错题复盘 |
| 考公考编 | 文昌功名、启文昌助功名 | 官方资格、岗位筛选、模考和面试训练 |
| 事业 | 事业功名、官禄、贵人、面试或签约择时 | 可验证成果、雇主核验、合同审阅 |
| 健康 | 安康、静心、净宅 | 医疗评估优先，急重症及时求助 |
| 风水 | 安宅、净宅、照明通风和低风险布局 | 户型、可靠测量、建筑与消防安全 |
| 迁移 | 迁移择时作为材料复核节点 | 官方签证、预算、语言、住房和返程方案 |
| 艺术与技能 | 开笔或开工作为练习启动 | 训练记录、作品迭代和外部反馈 |

## 4. 匹配优先级

建议选择使用以下顺序：

1. 先按 `domain` 隔离，不能跨专题混入；
2. 只保留 `status=production`；
3. 先检查现实禁忌与高风险条件；
4. 命中安全拒绝规则时，只返回拒绝和现实处置；
5. 再匹配显式需求；
6. 最后返回领域默认低风险建议。

### 4.1 财务 fail closed

出现 `cashflow_status=critical` 或 `borrowing_for_ritual=true` 时，只返回“当前不建议具体仪式”，预算上限为零，并要求先处理基本生活、现金流和债务。系统不会因为财运专题就默认建议补财库。

补财库相关规则只有在 `ritual_interest` 明确指向该主题时才可匹配；它仍受现金流、预算和单次频率限制。

### 4.2 健康 fail closed

有症状时，建议正文必须出现医疗优先。静心、安康和净宅不能延误、替代或否定医疗评估。

### 4.3 风水 fail closed

缺少户型、用途和可靠测量时，只能给清洁、通风、照明、动线和安全提醒，不能给坐向或方位结论。

## 5. 禁止宣称

任何 AdviceRule 或 Renderer 都不得：

- 包上岸、保证复合、保证发财；
- 宣称治疗、替代医疗或阻止就医；
- 用灾祸、家人受害或关系破裂恐吓用户；
- 建议借钱、贷款或挪用基本生活费用；
- 用付款截图、好评或作者自述证明有效；
- 先低价引流，再以“未做完整”为由连续追加。

## 6. 诈骗红旗与退出机制

默认红旗：要求借钱、灾祸恐吓、结果承诺、价格或次数不透明。用户出现任一红旗时，应停止文化建议并回到现实处置。

所有建议必须：

- 可拒绝；
- 可随时退出；
- 次数有限；
- 价格上限预先可见；
- 不保存或索取无关私密资料；
- 不与支付金额、好评或续费绑定。

## 7. 来源、状态与审核

当前目录的直接来源是本次用户授权的付费交付合同、项目 `AGENTS.md` 安全边界、已有 Phase 16/17 现实优先专题结构，以及本治理文档。规则审核记录使用稳定 decision ID；它只证明结构、安全和调用范围经过本地审查。

传统文化建议若要改变为经验性效果宣称，必须另立 forward test，冻结预测和条件，等待到期并接受独立现实反馈。代码不会把 `raw`、`draft`、`reviewed` 或 `pending_forward_test` 自动升级为 `production`。

## 8. 当前限制

本模块不执行仪式、不接支付、不推荐服务商、不调用外部网络、不记录真实用户资料，也不提供真实方位计算。它只返回结构化、本地、确定的文本建议。
