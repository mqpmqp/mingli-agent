# 批次导入与回滚

批次 ID 由输入相对路径、输入校验和与工具版本生成，重复执行返回同一 manifest。manifest 记录输入、输出、生命周期、对象数量、验证结果与回滚计划。

回滚先比对当前输出哈希。任何人工改动都会终止自动回滚。`--dry-run` 只报告计划；真实回滚只删除 manifest 明列的新文件，并按快照恢复被修改文件。报告输出到标准输出，避免在已恢复工作区留下额外文件。
