# 置信度与反证合同

版本：`confidence-and-counterevidence@1.0`
实现：`mingli.paid_delivery.build_confidence_profile`

## 1. 四维置信度

置信度不得压成单一总分，必须分别回答四个问题：

| 维度 | 判断对象 |
|---|---|
| `chart_confidence` | 输入是否可靠、排盘或静态事实是否由确定性算法得到 |
| `interpretation_confidence` | 规则状态、证据链和现实反证是否支持当前解释 |
| `event_confidence` | 现实事件资料是否完整、外部变量是否可控 |
| `action_confidence` | 行动建议的前置条件是否满足、风险是否可控 |

输出值固定为 `high`、`medium`、`low`。没有经过校准与独立验证时，不输出概率、胜率或类似统计包装。

## 2. 高置信门禁

高置信必须同时满足：

- 输入可靠；
- 算法确定；
- 所有进入判断的规则均为 `production`；
- 至少存在一条明确证据链；
- 没有强现实反证；
- 对 event/action 维度，还要现实事件资料完整且行动前置条件满足。

任一条件不满足就不能维持对应维度的高置信。

## 3. 中置信

以下情况进入中置信：

- 存在一个重要不确定因素；
- 关系、招录、岗位或迁移受外部主体或制度变量影响；
- 命盘和规则可用，但现实资料只支持条件性解释；
- 证据链有支持，也需要保留反证。

中置信不是“接近确定”，而是“可以形成条件性判断，但必须复盘”。

## 4. 低置信与自动降级

以下情况固定降级：

- 出生时间、地点、对象、目标问题或现实资料缺失；
- 图片四柱未确认；
- 只能看到趋势，不能连接到具体事件；
- 六爻资料存在但可靠引擎不可用；
- 规则不是 `production`；
- 缺少证据链；
- 强现实反证与象意冲突；
- 行动前置条件尚未满足。

命盘维度为低时，解释、事件和行动维度不能绕过它升高。

## 5. 证据链

新付费路径的证据链由稳定摘要组成：

- `chart:<runtime hash>`：确定性命盘或已确认静态四柱收据；
- `reality:verified:<hash>`：已核验现实资料；
- `reality:self-reported:<hash>`：尚未独立核验的用户自述；
- `rule:<rule hash>`：实际进入判断的 production 规则。

证据链证明“使用了什么”，不证明“预测已经命中”。付款截图、作者自述、泛化描述和到期前的反馈不能提升证据质量。

## 6. 现实反证优先

现实反证按专题处理，不做全局模糊覆盖。

### 感情

强反证包括对方已婚、明确拒绝、联系渠道被屏蔽、安全风险和法律限制。新人、长期失联和家庭阻力至少构成重要限制。出现强反证时，事件与行动置信度降级，输出必须直接写明现实边界。

### 考公考编

专业、年龄、学历等官方资格不符属于硬反证。成绩、岗位竞争和招录目标缺失时不能给出上岸强结论。

### 财务

现金流紧张、准备借款支付文化服务属于硬反证。此时仪式建议被安全拒绝规则替换。

### 健康

存在症状但没有医疗评估时，行动置信度受限；医疗处置优先于任何传统辅助。

### 风水

缺户型或可靠测量时，资料不足本身就是反证，只能给低风险环境整理建议。

## 7. 成立条件、反证与复盘

每条重大判断必须携带：

- 成立条件：哪些输入和现实前提需要持续为真；
- 反证：哪些事实一旦出现会使结论降级或撤回；
- 复盘点：何时、按什么标准重新核对。

时间性判断没有复盘点时不得进入交付。当前实现没有 forward calibration 的具体日期依据，因此主动拒绝具体日期，只保留观察窗口和复盘点。

## 8. 与既有系统的关系

本合同只约束新 `paid_delivery` 路径，不静默改变既有 Phase 8、Phase 18 或 Phase 23 的置信度字段和旧规则状态。旧系统可继续兼容运行；新付费路径不会把旧 `reviewed` 或 `verified` 自动视为新合同的 `production`。
